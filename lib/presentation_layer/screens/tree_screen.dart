import 'package:flutter/material.dart';

class TreeScreen extends StatelessWidget {
  const TreeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Tree Screen'),
    );
  }
}
